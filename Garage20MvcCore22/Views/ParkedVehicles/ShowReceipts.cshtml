@model List<string>
      


<h1> Receipts </h1>

@foreach(var element in Model)
    {
<p>
    @Html.DisplayFor(n => element) <a asp-action="ReceiptDetails" asp-route-fileName="@element">View</a>
    @*@Html.DisplayFor(n => element)
    @Html.ActionLink(element, "ReceiptDetails")*@
</p>
}
